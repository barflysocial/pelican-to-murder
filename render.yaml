services:
  - type: web
    name: pelican-to-murder
    env: node
    buildCommand: npm cache clean --force && npm install --no-audit --no-fund
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.18.1
