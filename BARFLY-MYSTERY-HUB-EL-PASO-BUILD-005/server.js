const path=require('path');const fastify=require('fastify')({logger:false});fastify.register(require('@fastify/static'),{root:path.join(__dirname,'public'),prefix:'/'});fastify.setNotFoundHandler((req,reply)=>reply.sendFile('index.html'));const port=process.env.PORT||3000;fastify.listen({port,host:'0.0.0.0'},err=>{if(err){console.error(err);process.exit(1)}console.log('Barfly Mystery Hub running on '+port)});
